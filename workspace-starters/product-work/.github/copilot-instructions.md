This is an independent product-work workspace. Follow `AGENTS.md`, `WORKSPACE.md`, and `AI-WORKFLOW.md`. Use only approved evidence, cite paths, separate facts from inference, and keep client decisions with people. Put rough work in `working-notes/` and reviewable outputs in `deliverables/`. Do not modify attached client code or push anywhere unless the user explicitly authorizes it. Never use the canonical ProductLearning repository as this workspace's remote.
